[package]
name = "behaviortree"
version = "0.1.5"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["rc", "derive"] }
tokio = { version = "1", default-features = false, features = [
    "macros",
    "sync",
] }
