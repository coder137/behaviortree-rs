# BehaviorTree Future

# CI

```
cargo install --force cargo-make
cargo install nextest
cargo install llvm-cov

cargo make ci
```

# Dev

```
cargo install --force cargo-make
cargo install nextest
cargo install llvm-cov

cargo make coverage-view
```
